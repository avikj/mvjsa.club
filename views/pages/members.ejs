<?php

include_once 'header.php';
include_once 'mysql_connect.php';

if(isset($user)) {
	if(isset($_REQUEST['action']) && $_REQUEST['action']=="passchange") {
		include_once 'passchange.php';
	}
	else if(isset($_SESSION['admin'])) {
		if(isset($_REQUEST['action']))
			include_once $_REQUEST['action'].'.php';
		else
			include_once 'admin_portal.php';
	}
	else
		include_once 'members_portal.php';
}
else
	include_once 'members_login.php';

include_once 'footer.php';

?>